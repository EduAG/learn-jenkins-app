pipeline {
    agent any

    stages {

        stage('Build Docker images'){
            steps {
                // Building the Docker image using Dockerfile.my-playwright
                sh 'docker build -t my-playwright -f ci/Dockerfile.my-playwright.'
                sh 'docker build -t my-aws -f ci/Dockerfile.aws .' 
            }
        }
    }
}